{
  "name": "bluetooth-scanner",
  "version": "1.0.0",
  "description": "Scans for known devices on a regular basis, use MQTT to trigger extra scans, and publishes a message when a device is either detected or leaving",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "await-spawn": "^4.0.1",
  }
}
